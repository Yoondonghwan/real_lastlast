<h1></h1>
<div class= 'container  w3-teal'>

<% if current_user.nil? %>
    <%= link_to '!로그인!', new_user_session_path %>
<% else %>
    <%= current_user.email %>
    <%= link_to '　　로그아웃', destroy_user_session_path, method: 'delete' %>
<% end %>
</div>

<h1>뒷담화 게시판</h1>

<d>
    <table class="table ">
        <thead>
      <tr>
        <th>제목</th>
        <th>수정?!</th>
      </tr>
    </thead>
        <tbody>
        <% @posts.each do |post| %>
        <tr>
            <td><a href="/posts/show/<%= post.id %>"><%= post.title %></a></td>
            <td><a href="/posts/edit/<%= post.id %>"> clik_me </a></td>
            
            <%end%>
        </tr>
        </tbody>
    </table>
    <a class="btn btn-default" href="/posts/new" role="button">새글작성하기</a>